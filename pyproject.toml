[project]
name = "tiny-doom-defender"
version = "0.1.0"
description = "1.1M ModernBERT encoder playing DOOM in real-time in CPU"
requires-python = ">=3.10"
dependencies = [
    "torch",
    "transformers",
    "numpy",
    "Pillow",
    "datasets",
    "vizdoom",
    "gymnasium",
]

[project.scripts]
create-model = "tiny_doom_defender.scripts.create_model:main"
record-oracle = "tiny_doom_defender.scripts.record_oracle:main"
train-sft = "tiny_doom_defender.scripts.train_sft:main"
train-ppo = "tiny_doom_defender.scripts.train_ppo:main"
select-ppo-snapshots = "tiny_doom_defender.scripts.select_ppo_snapshots:main"
eval-model = "tiny_doom_defender.scripts.eval_model:main"
play-doom = "tiny_doom_defender.scripts.play_doom_visual:main"

[build-system]
requires = ["uv_build>=0.11,<0.12"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "ruff>=0.15.21",
]

[tool.ruff]
line-length = 120
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP", "B"]
